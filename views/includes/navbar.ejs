<style>
/* 🌍 WanderStay Navbar Styling */
.navbar {
  background-color: #fff;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
  padding: 1rem 2.5rem;
  font-family: 'Poppins', sans-serif;
  transition: all 0.3s ease;
  z-index: 100;
}

/* Brand */
.navbar-brand {
  display: flex;
  align-items: center;
  font-weight: 600;
  color: #222;
  text-decoration: none;
}

.navbar-brand i {
  font-size: 1.8rem;
  color: #ff385c;
  margin-right: 8px;
  transition: transform 0.3s ease;
}

.navbar-brand:hover i {
  transform: rotate(-10deg) scale(1.1);
}

/* Center Nav */
.navbar-nav .nav-link.brand-text {
  font-size: 1.1rem;
  font-weight: 600;
  color: #ff385c !important;
  letter-spacing: 1px;
  position: relative;
  transition: color 0.3s ease;
}

.navbar-nav .nav-link.brand-text:hover {
  color: #e23153 !important;
}

/* Search Bar */
.search-bar {
  display: flex;
  align-items: center;
  background-color: #fff;
  border: 1px solid #ddd;
  border-radius: 50px;
  padding: 0.4rem 1rem;
  min-width: 300px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
  transition: box-shadow 0.3s ease, border-color 0.3s ease;
}

.search-bar:focus-within {
  box-shadow: 0 0 0 3px rgba(255, 56, 92, 0.25);
  border-color: #ff385c;
}

.search-bar input {
  border: none;
  outline: none;
  flex: 1;
  font-size: 0.95rem;
  background: transparent;
}

.search-btn {
  background-color: #ff385c;
  color: #fff;
  border: none;
  border-radius: 50%;
  width: 34px;
  height: 34px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background-color 0.3s ease, transform 0.2s ease;
}

.search-btn:hover {
  background-color: #e23153;
  transform: translateY(-1px);
}

/* Right Side */
.navbar-nav.ms-auto .nav-link {
  margin-left: 0.8rem;
  color: #444;
  font-weight: 500;
  border-radius: 25px;
  transition: all 0.3s ease;
}

.navbar-nav.ms-auto .nav-link:hover {
  background-color: #f7f7f7;
  color: #000;
  transform: translateY(-1px);
}

/* 👤 Username styling */
.username {
  font-weight: 600;
  color: #ff385c;
  padding: 0.5rem 1rem;
  border-radius: 20px;
  background-color: #fff5f6;
  margin-right: 0.5rem;
  display: flex;
  align-items: center;
}

.username i {
  margin-right: 6px;
  color: #ff385c;
}

@media (max-width: 768px) {
  .navbar {
    padding: 0.7rem 1.3rem;
  }
  .search-bar {
    min-width: 180px;
  }
}
</style>

<nav class="navbar navbar-expand-md sticky-top">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand" href="/">
      <i class="fa-solid fa-compass"></i>
      <span>WanderStay</span>
    </a>

    <!-- Toggler -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      
      <div class="navbar-nav mx-auto">
        <a class="nav-link brand-text" href="/listings">Discover Stays</a>
      </div>

      <div class="navbar-nav">
        <form class="search-bar" role="search">
          <input type="search" placeholder="Search destinations..." />
          <button class="search-btn" type="submit">
            <i class="fa-solid fa-search"></i>
          </button>
        </form>
      </div>

      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">
          <i class="fa-solid fa-house"></i> List your stay
        </a>

        <% if (!currUser) { %>
          <a class="nav-link" href="/signup"><b>Sign Up</b></a>
          <a class="nav-link" href="/login"><b>Login</b></a>
        <% } else { %>
          <div class="username">
            <i class="fa-solid fa-user"></i>
            <%= currUser.username %>
          </div>
          <a class="nav-link" href="/logout"><b>Logout</b></a>
        <% } %>
      </div>

    </div>
  </div>
</nav>
